{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "alfa-express",
      "runtimeExecutable": "powershell",
      "runtimeArgs": [
        "-NoProfile",
        "-Command",
        "$r='C:\\Users\\natus\\Desktop\\Site\\alfa_experess'; $m=@{'.html'='text/html; charset=utf-8';'.css'='text/css; charset=utf-8';'.js'='application/javascript';'.svg'='image/svg+xml';'.png'='image/png';'.jpg'='image/jpeg';'.jpeg'='image/jpeg';'.ico'='image/x-icon';'.json'='application/json';'.webp'='image/webp'}; $l=New-Object Net.HttpListener; $l.Prefixes.Add('http://localhost:3000/'); $l.Start(); Write-Output 'Serving on http://localhost:3000/'; while($l.IsListening){ try{ $c=$l.GetContext(); $p=[Uri]::UnescapeDataString($c.Request.Url.AbsolutePath.TrimStart('/')); if(-not $p){$p='index.html'}; $f=Join-Path $r $p; if(Test-Path $f -PathType Container){$f=Join-Path $f 'index.html'}; $f=[IO.Path]::GetFullPath($f); if($f.StartsWith($r) -and (Test-Path $f -PathType Leaf)){ $e=[IO.Path]::GetExtension($f).ToLower(); if($m[$e]){$c.Response.ContentType=$m[$e]}; $b=[IO.File]::ReadAllBytes($f); $c.Response.ContentLength64=$b.Length; $c.Response.OutputStream.Write($b,0,$b.Length) } else { $c.Response.StatusCode=404 }; $c.Response.Close() } catch {} }"
      ],
      "port": 3000
    }
  ]
}
